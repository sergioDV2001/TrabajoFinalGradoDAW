<%- include("template/cabecera", { title: "Cuanto sabes de Japon", style: "servicios" }) %> <%-
include("template/navbar") %>


<div class="container my-5 ">

    <div class="row text-center">

        

        <div class="col-12 col-md-6">

            <form class="mt-5" action="/jugarPartida" method="get">
            
            <h1>Bienvenido a cuanto sabes de <span class="rojo">Japón</span></h1>
            <p class="mt-5">En este juego vamos a poner a prueba todos tus <span class="rojo">conocimeintos</span> acerca de Japón,
            para saber cuanto sabes del pais al que quieres visitar. <span class="rojo">¿Quieres probar?</span></p>

            <h1 class="text-center mt-5 rojo">Usuario</h1>
            
            <p class="mt-3">¡Introduce tu <span class="rojo">nombre de usuario</span> y podremos empezar a ponerte a prueba!</p>

            <% if (usuario == "") { %>
             
                <p class="mt-3">Debes de <a href="/login"><span class="rojo">inciar sesion</span></a> para poder jugar.</p>

            <% } %>

            


                


                <button type="submit" class="btn btn-danger w-100 mt-3">Jugar</button>

                <small class="text-muted">
                    CONDICIONES LEGALES 
                    <a class="text-muted">Cookies</a>
                    <a class="text-muted">Uso y Privacidad</a>
                </small>

                </form>
            
        
        </div>

        <div class="col-12 col-md-6">
            
            <h1 class="mt-5 rojo">Tabla de clasificación</h1>

            <table class="table mt-5">
                <thead>
                  <tr>
                    <th class="rojo">Puesto</th>
                    <th class="rojo">Usuario</th>
                    <th class="rojo">Puntos</th>
                  </tr>
                </thead>

                <% let i = 1 %> 
                <% arrayPuntuacion.forEach(puntuacion => { %>
                    
                    <% if (i <= 10) { %>
                     
                        <tr>
                            <td><%= i++  %> </td>
                            <td><%= puntuacion.usuario %></td>
                            <td><%= puntuacion.puntos  %></td>
                        </tr>

                    <% } %>

                    
                    <% }) %>
                
            </table>

        </div>


    </div>

</div>




<%- include("template/footer") %>